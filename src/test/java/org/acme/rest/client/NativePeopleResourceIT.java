package org.acme.rest.client;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativePeopleResourceIT extends PeopleResourceTest {

    // Execute the same tests but in native mode.
}